def oddTuples(aTup):
    '''
    aTup: a tuple
    
    returns: tuple, every other element of aTup. 
    '''
    # Your Code Here 
    Tup=()
    index = 0
  
    for i in range(len(aTup)):
        if index%2 == 0:
           Tup=Tup + (aTup[i],)
        index = index + 1
    return Tup    
